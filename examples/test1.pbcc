transform Test1 from IN[w,h] to OUT[w,h] 
testcase A
{
  OUT.cell(x,y) from(OUT.cell(x-1, y) left,
                     OUT.cell(x, y-1) up)
  {
     return left+up;
  }
  
  secondary OUT.cell(x,y) from()
  {
    return 1;
  }
}



