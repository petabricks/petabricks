%{
int thisIsATestOfFilePrefix=0;
%}

#define TEST define

transform Test1 from IN[w,h] to OUT[w,h] 
{
  OUT.cell(x,y) from(IN.cell(x,y)     i,
                     OUT.cell(x-1, y) left,
                     OUT.cell(x, y-1) up)
  {
     return i,left+up;
  }
  
  secondary OUT.cell(x,y) from(IN.cell(x,y) i)
  {
    return i;
  }
}



