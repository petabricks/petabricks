transform Copy1D
from IN[n]
to OUT[n]
{
  OUT.cell(i) from (IN.cell(i) x)
  {
    return x;
  }
}

transform Copy2D
from IN[w,h]
to OUT[w,h]
{
  OUT.cell(i,j) from (IN.cell(i,j) x)
  {
    return x;
  }
}

