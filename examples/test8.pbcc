transform SOR2D
from IN[n,n], B[n,n] 
through R[n+2,n+2], K[n+2,n+2]
to OUT[n+2,n+2]
{

  // INITIALIZE
  
  R.cell(i,j) from (IN.cell(0,0) in) { return 0; }

  K.cell(i,j) from (IN.cell(0,0) in) { return 0; }

  // FINALIZE
  
  OUT.cell(i,j) from(R.cell(i,j) x) {
    return x;
  }

}
