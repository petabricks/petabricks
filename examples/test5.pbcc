transform Add 
from A[w,h], B[w,h]
to OUT[w,h] 
{
  OUT.cell(x,y) from(A.cell(x,y) a, B.cell(x,y) b){
    return a+b;
  }
}

transform Test5
from IN[w,h] 
to OUT[w,h] 
{
  to(OUT o) from(IN i) {
    Add(o,i,i);
  }
}

