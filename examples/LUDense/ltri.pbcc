transform ltri
from IN[n, n]
to OUT[n, n]
{
    to   (OUT.cell(i, j) out)
    from ( IN.cell(i, j) in)
    {
        if (i > j) {
            out = in;
        } else if (i == j) {
            out = 1;
        } else {
            out = 0;
        }
    }
}

