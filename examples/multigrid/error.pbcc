//calculate root mean square errors

transform error
from A[m,n], B[m,n] 
to er
{
  er from(A a, B b){
    ElementT sum=0, tmp=0;
    for(IndexT i=0; i<m; ++i){
	for(IndexT j=0; j<n; ++j){
		tmp=a.cell(i,j)-b.cell(i,j);
		sum+=tmp*tmp;
	}
    }
    return sqrt(sum/(m*n)); 
  }
}
